# 01-simple-agent

在这一章节中，我们会使用不同的Agent开发框架去搭建一个最简单的Agent，

这一章节中，我将会搭建一个最简单的 Agent，它有以下特点：

1. 以简单的单个任务为导向
2. 使用 MCP 作为外接工具的统一协议

为了~~让自己~~让大家更好地比较各个框架之前的差异，我会同时使用不同的框架进行示例。
此外，在后文中，我会只使用 MCP 作为工具的协议，避免混乱。
